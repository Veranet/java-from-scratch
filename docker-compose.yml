version: "3.9"

services:
  db:
    image: postgres:16.3
    environment:
      POSTGRES_DB: my_ticket_service_db
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"
